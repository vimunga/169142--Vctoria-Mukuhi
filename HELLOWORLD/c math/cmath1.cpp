// calculating the square rooot of 25 and logarithm of log 2
#include <iostream>
#include <cmath>

using namespace std;
int main()
{
    int x = -10;
    cout << " Absolute value of " << x << " is" << abs(x) <<endl;

    //initialize new variable to 25
    int m = 25;
    
    //computing the square root of 25
    cout << " the square root of " << m <<"is " << sqrt(m) << endl;
    // initializing a variable z to 2
    int z =  2;
   //compute the log arithm of z
    cout << "the log of base "<< z << "is " << log(z) << endl;
    // computing 2 raised to the power 3
    cout <<" 2^3b= "<<pow(2,3)  << endl;

}